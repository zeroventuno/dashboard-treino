{
  "name": "trak-coach-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TRAK coach MCP server — the write path from any LLM chat into a tenant's training data.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "migrate": "node migrate.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "@vercel/node": "^5.8.23",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
